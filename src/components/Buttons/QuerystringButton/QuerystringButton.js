import React from 'react';
import './QuerystringButton.scss';

const QuerystringButton = () => {
  return (
    <div className="querystring">
      <button className="querystring__btn">
        <span>Querystring</span>
      </button>
    </div>
  );
};

export default QuerystringButton;
